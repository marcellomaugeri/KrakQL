services: 

  react-finland:
    platform: linux/amd64
    image: react-finland
    build:
      dockerfile: ./Dockerfile
    ports:
      - ":3000"
    environment:
      NODE_ENV: "production"
    command: yarn start