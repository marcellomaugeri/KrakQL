services: 

  react-finland:
    build:
      dockerfile: ./Dockerfile
    container_name: react_finland
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: "production"
    command: sh -c "timeout 60 npm start"